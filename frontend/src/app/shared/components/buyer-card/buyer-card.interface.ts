// import { KeyValue } from '@angular/common';

export interface BuyerCardI {
  oid: string;
  // contextMenuItems: KeyValue<string, string>[];
  // firstName?: string;
  // lastName?: string;
  // email?: string;
  // userInitials?: string;
  // thumbnail: string;
}
